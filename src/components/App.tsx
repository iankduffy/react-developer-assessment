import { Home } from "./homepage";
import { Layout } from "./shared/layout";

const App: React.FC = () => {
  return (
    <Layout>
      {/* Complete the exercise here. */}
      <Home />
    </Layout>
  );
};

export default App;
